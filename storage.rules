rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /verification_certificates/{accID} {
       allow write: if request.auth != null;
       allow read: if request.auth != null; // Admin/user can read
    }
  }
}
