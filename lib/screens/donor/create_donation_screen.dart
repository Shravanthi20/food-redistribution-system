import 'package:flutter/material.dart';

class CreateDonationScreen extends StatelessWidget {
  const CreateDonationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Donation'),
      ),
      body: const Center(
        child: Text('Create Donation Screen - To be implemented'),
      ),
    );
  }
}