import 'package:flutter/material.dart';

class NGORegistrationScreen extends StatelessWidget {
  const NGORegistrationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('NGO Registration'),
      ),
      body: const Center(
        child: Text('NGO Registration Screen - To be implemented'),
      ),
    );
  }
}